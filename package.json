{
    "name": "privy-flow-monorepo",
    "version": "0.0.1",
    "private": true,
    "workspaces": [
        "web",
        "backend",
        "contracts",
        "shared",
        "scripts"
    ],
    "scripts": {
        "dev": "pnpm -r run dev",
        "build": "pnpm -r run build",
        "test": "pnpm -r run test",
        "lint": "pnpm -r run lint"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "ts-node": "^10.9.1"
    }
}